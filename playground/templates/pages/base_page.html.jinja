{% extends 'base.html.jinja' %}
{% block header %}{{ results.title }}{% endblock %}
{% block content %}
<div class="row justify-content-center">
    <div class="p-2 mb-4 bg-body-tertiary rounded-3 col-10">
        <div class="container-fluid py-5">
            <div class="col-md-12">
            {% if results.html %}
                {{ results.html }}
            {% endif %}
            </div>
        </div>
    </div>
</div>
{% endblock %}